spring:
  application:
    name: company-service
  datasource:
    url: jdbc:postgresql://localhost:5444/postgres
    password: seyha123
    username: seyha
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672
server:
  port: 9000